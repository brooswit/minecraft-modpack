name: Update Village Flag

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Village Flag version to update to (leave empty for latest)'
        required: false
        default: ''
  repository_dispatch:
    types:
      - village-flag-updated

jobs:
  update:
    name: Update Village Flag in modpack
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packwiz
        run: |
          go install github.com/packwiz/packwiz@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Update village-flag
        run: packwiz update village-flag

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update village-flag mod\n\nCo-Authored-By: Paperclip <noreply@paperclip.ing>"
          branch: main
