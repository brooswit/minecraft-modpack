name: Update Cafe Table

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Cafe Table version to update to (leave empty for latest)'
        required: false
        default: ''
  repository_dispatch:
    types:
      - cafe-table-updated

jobs:
  update:
    name: Update Cafe Table in modpack
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install packwiz
        run: |
          go install github.com/packwiz/packwiz@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Update cafe-table
        run: packwiz update cafe-table

      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update cafe-table mod\n\nCo-Authored-By: Paperclip <noreply@paperclip.ing>"
          branch: main
